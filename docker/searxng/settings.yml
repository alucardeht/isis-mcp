use_default_settings: true

general:
  instance_name: "ISIS-MCP SearXNG"

search:
  formats:
    - html
    - json
  safe_search: 0
  autocomplete: ""

server:
  port: 8080
  bind_address: "0.0.0.0"
  secret_key: "isis-mcp-local-key-12345"
  limiter: false
  image_proxy: false

# Disable bot detection for local API use
enabled_plugins: []

redis:
  url: false
